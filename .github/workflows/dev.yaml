name: Repository Variables

on:
  push:
    branches:
      - dev

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Repository Variables
        run: |
          echo "Building and testing the application"
          echo "${{ toJSON(vars) }}"

      - name: Specific Variable
        run: |
          echo "${{ toJSON(vars.TEST_ENV) }}"
